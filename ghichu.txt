http://localhost:5173/admin.html 
http://localhost:5173



git pull origin main
Sau khi pull xong, restart Docker để áp dụng thay đổi:

docker compose restart backend
Hoặc nếu họ có thêm thư viện mới (sửa requirements.txt) thì cần rebuild:

docker compose up -d --build




Frontend (React/Vite): Tự động hot reload, không cần chạy lệnh gì

Backend (Python):

docker-compose restart backend
Thêm package mới (requirements.txt hoặc package.json):

docker-compose up --build
Thêm migration database: docker-compose exec backend flask db upgrade

docker-compose exec backend python seed_data.py


Số thẻ: 9704 0000 0000 0018
Tên: NGUYEN VAN A
Ngày hết hạn: 03/07
0379991650
OTP: OTP